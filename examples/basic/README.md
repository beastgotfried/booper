# Basic Example

This directory is a small, readable Python target for exercising the harness.

```bash
enshittify run examples/basic \
  --profile maximum \
  --intensity maximum \
  --output archive
```

The command leaves this directory unchanged and writes the degraded copy beneath `.enshittify/runs`.
